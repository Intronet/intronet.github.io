# Project Todo List

## Refactoring
- [ ] Rename 'RHYTHM' to 'RECORDING' globally.
    - [ ] UI Labels: Update tab names in `EffectsPanel`.
    - [ ] Store Keys: Rename `enginePanelTab` value from `'rhythm'` to `'recording'`, and `instrumentStore.rhythmPatterns` to `recordingPatterns`.
    - [ ] Components: Rename `RhythmDesignPanel`, `RhythmDesignCanvas`, `RhythmDesignToolbar` to `Recording...`.
    - [ ] Hooks: Rename `useRhythmDesign` to `useRecordingDesign`.
